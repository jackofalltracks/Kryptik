<%= simple_form_for(@status) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :user_id %>
    <%= f.input :content %>
    <%= f.input :index %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
