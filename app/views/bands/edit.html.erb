<h1>Editing <%= @band.name %></h1>

<%= render 'form' %>

<%= link_to 'Show', @band %> |
<%= link_to 'Back', bands_path %> |
<%= link_to 'Drop Band', @band, method: :delete, data: { confirm: 'Are you sure?' } %>
